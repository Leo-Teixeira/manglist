import 'package:flutter/material.dart';

class MorePublicationsMangas extends StatelessWidget {
  const MorePublicationsMangas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
